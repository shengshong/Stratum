[package]
name = "stratum-boot-health"
version = "0.1.0"
edition = "2021"
description = "Post-boot security stack verification for AB-i9"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
